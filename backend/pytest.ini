[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    unit: tests unitarios sin servicios externos
    integration: tests que cruzan modulos o servicios locales
    db: tests que requieren TEST_DATABASE_URL y una base de datos aislada
