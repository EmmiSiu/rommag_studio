# Dependencias de desarrollo/test del backend.
# Mantener separadas de requirements.txt evita instalarlas en la imagen runtime.
pytest>=9.0,<10.0
pytest-asyncio>=1.3,<2.0
pytest-cov>=7.0,<8.0
httpx>=0.28,<1.0
